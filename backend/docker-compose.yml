services:
  db:
    image: mysql:8.0
    container_name: chamado_db_mysql
    restart: always
    environment:
      MYSQL_DATABASE: db_suporte
      MYSQL_ROOT_PASSWORD: root
    ports:
      - '3306:3306'